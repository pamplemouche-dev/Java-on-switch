APP_TITLE := JavaSwitch
APP_AUTHOR := Pamplemouche
APP_VERSION := 1.0.0
# On utilise le fichier que tu as créé sur GitHub
NACPFLAGS := --control=$(CURDIR)/assets/control.nacp
ICON := $(CURDIR)/assets/icon.jpg
